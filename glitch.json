{
  "start": "java -cp target/classes:'target/dependency/*' Main",
  "install": "echo 'compiling' && mvn compile",
  "watch": {
    "throttle": 500,
    "install": {
      "include": [
        "^pom\\.xml$",
        "\\.java$"
      ]
    }
  }
}
